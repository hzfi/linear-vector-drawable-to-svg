# Env

`Node.js v24.0.2`

## `L`inear / radial gradient `V`ector `D`rawable `2` `SVG` converter

linear-gradient or radial-gradient android vector drawable to SVG converter.

## Usage

```bash
cd drawable
lvd2svg
```
